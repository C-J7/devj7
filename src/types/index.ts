export type Certificate = {
    id: number;
    title: string;
    issuer: string;
    date: string;
    credentialLink: string;
  };
  
  export type NavLink = {
    name: string;
    href: string;
  };